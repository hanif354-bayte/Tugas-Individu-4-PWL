// roleMiddleware.js
const roleMiddleware = (role) => {
    return (req, res, next) => {
      // Check if user has the required role
      // If yes, allow access; otherwise, deny
    };
  };
  
  module.exports = roleMiddleware;
  